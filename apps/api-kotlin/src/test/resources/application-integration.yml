snapplay:
  demo: false

# Exclude OAuth2 resource server — no JWKS needed for repository-level tests
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration
